<%= form_with(model: ventum, local: true) do |form| %>
  <% if ventum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ventum.errors.count, "error") %> prohibited this ventum from being saved:</h2>

      <ul>
      <% ventum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :usuario_id %>
    <%= form.text_field :usuario_id, id: :ventum_usuario_id %>
  </div>

  <div class="field">
    <%= form.label :vendedor_id %>
    <%= form.text_field :vendedor_id, id: :ventum_vendedor_id %>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.date_select :fecha, id: :ventum_fecha %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
